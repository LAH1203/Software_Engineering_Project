doctype html 
html
    head
        title 후기 입력
    body
        h1 후기 입력
        form(action="/setreview" method="post")
            label 캠핑장 id 
            input(value=`${id}` name='id' readOnly)
            p
                h3 후기 입력란
                
                textarea(name="description", cols="30", rows="10")
            p 
                h3 사진 업로드
                
                input(type="file")
            p 
                h3 별점 
                
                input(type="number" min="1" max="5" step="1" value="1") 

            button(type='submit') 작성
            button(type='button' onclick="location.href='/mypage'") 취소